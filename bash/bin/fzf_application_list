#!/bin/bash

title="Fuzzy Finder"

_get_selection() {
	compgen -c | grep -E "^.*[[:alnum:]]+.*$" | fzf --exact --height=10 --reverse
}

_main() {
	if selection=$(_get_selection)
	then
		nohup "$selection" &>/dev/null &
		notify-send "$title" "Running $selection"
	fi
}

_main
