#!/bin/bash

title="FZF Launcher"

_get_selection() {
	find ~ -type f 2>/dev/null | fzf --height=10 --reverse
}

_main() {
	if selection=$(_get_selection)
	then
		nohup gnome-open "$selection" &
		notify-send "$title" "Opening $selection"
	fi
}

_main
