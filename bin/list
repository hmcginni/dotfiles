#!/bin/bash

filePath="$(which $1)"
fileLength=($(wc -l $filePath))
LINES=$(echo $(tput lines))

if (( fileLength[0] < LINES - 5 )); then
    nl -ba $filePath
    exit 0
else
    less $filePath
fi

 
