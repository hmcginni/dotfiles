#!/bin/bash
#
# Delete Temporary Files

root_dir="$HOME"
org_dir="$root_dir"/org

notify-send "rmtemp" "Deleting temp files ..."

find "$root_dir" -type f -name "*[~#]" -delete 2>/dev/null
find "$root_dir" -xtype l -delete 2>/dev/null
find "$org_dir" -type f -name "*.html" -delete 2>/dev/null
