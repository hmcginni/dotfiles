#!/bin/bash
#
# Delete Temporary Files

rootDir="$HOME"
notify-send "rmtemp" "Deleting temp files ..."
find "$rootDir" -type f -name "*[~#]" -delete 2>/dev/null
find "$rootDir" -xtype l -delete 2>/dev/null

